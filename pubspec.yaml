name: startup_namer
description: Primeiro app com Flutter e Android Studio.

#publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  english_words: ^ 4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

  firebase_core: "^0.7.0"
  firebase_auth: "^0.20.1"

flutter:

  uses-material-design: true
